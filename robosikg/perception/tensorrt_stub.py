from __future__ import annotations

"""
TensorRT stub: the architecture supports TensorRT, but the starter demo uses TorchVision.

TensorRT Python API supports parsing models (e.g., ONNX) and running plan (engine) files via `tensorrt`. 
See NVIDIA TensorRT Python API docs for build/runtime patterns. 
"""

# Intentionally left as a stub to keep the Cookoff starter runnable without TensorRT.
